from setuptools import setup
setup(name='AsciiArt',
      version='0.2.dev',
      description='Ascii "art" from images',
      author='malcolm',packages=['asciiart'],
      install_requires=['pillow'])